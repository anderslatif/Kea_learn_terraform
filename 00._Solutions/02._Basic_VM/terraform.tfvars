# Note. This file should NEVER be pushed to VCS. I have forced added it despite the .gitignore to demonstrate the solution. 
vm_name     = "exampleVM"
vm_username = "azureuser"
vm_password = "SecurePassword123!"
